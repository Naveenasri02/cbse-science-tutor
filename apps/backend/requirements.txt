# Core
fastapi>=0.115
uvicorn[standard]>=0.34
websockets>=13.0
python-dotenv>=1.0
httpx>=0.27
python-multipart>=0.0.9

# LLM client (OpenAI-compatible, talks to vLLM on localhost:8002)
openai>=1.50

# RAG (Document Q&A) — Production Pipeline
chromadb>=0.5
PyMuPDF>=1.24
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1
olefile>=0.47
sentence-transformers>=3.0
pdfplumber>=0.11
rank_bm25>=0.2.2

# Numerics
numpy>=1.26

# ── Voice deps (STT/TTS) ──
# server.py loads these gracefully — missing modules just disable voice without crash.
nemo_toolkit[asr]>=2.0
soundfile>=0.12
kokoro-onnx>=0.5.0
onnxruntime-gpu>=1.18
nvidia-cudnn-cu12
easyocr>=1.7

# vLLM itself is installed separately in the Dockerfile (vllm==0.20.1).
