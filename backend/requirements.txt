# Core
fastapi>=0.115
uvicorn[standard]>=0.34
websockets>=13.0
python-dotenv>=1.0
httpx>=0.27
python-multipart>=0.0.9

# LLM client (OpenAI-compatible, talks to vLLM)
openai>=1.50

# STT (NVIDIA Parakeet TDT — runs in-process on GPU)
nemo_toolkit[asr]>=2.0
soundfile>=0.12

# TTS (Kokoro ONNX — runs in-process on GPU)
kokoro-onnx>=0.5.0
onnxruntime-gpu>=1.18
nvidia-cudnn-cu12

# RAG (Document Q&A)
chromadb>=0.5
PyMuPDF>=1.24
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1
olefile>=0.47
sentence-transformers>=3.0

# Infra (installed on GPU server, not in requirements)
# vllm>=0.18        — serves Qwen3-8B LLM on port 8002

numpy>=1.26
